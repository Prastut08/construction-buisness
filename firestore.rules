rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Categories collection — public read, owner-only write
    // For now, allow read/write for development
    // TODO: Add proper authentication rules for production
    match /categories/{categoryId} {
      allow read: if true;
      allow write: if true;
    }
  }
}
